# requirements_cuda.txt — mirrors requirements.txt but pins CUDA-backed torch
# Install with: pip install -r requirements_cuda.txt

# --- core data handling -------------------------------------------------------
numpy>=1.24
pandas>=2.0

# --- audio --------------------------------------------------------------------
soundfile>=0.12
librosa>=0.10
scipy>=1.11

# --- source format parsers (chapter 1) ----------------------------------------
lxml>=4.9
praatio>=6.0

# --- HTTP / downloads ---------------------------------------------------------
requests>=2.31
tqdm>=4.66

# --- plotting -----------------------------------------------------------------
matplotlib>=3.7
seaborn>=0.13

# --- notebooks ----------------------------------------------------------------
jupyter>=1.0
ipykernel>=6.25
jupytext>=1.14

# --- chapters 3+ --------------------------------------------------------------
# PyTorch CUDA 12.4 — install separately via:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# (index-url can't be expressed in a requirements file cleanly for all pip versions)
torch>=2.1
torchaudio>=2.1
transformers>=4.40
datasets>=2.18
accelerate>=0.27
scikit-learn>=1.3